#!/usr/bin/env python
# -*- coding: utf-8 -*-
import os
import re
import sys

from setuptools import setup, find_packages

setup(
    install_requires=[
        'django-wizard-builder==1.0.5',
    ],
)
